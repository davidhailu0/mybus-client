version: "3.7"
services:
  web:
    container_name: web
    build: ./
    stdin_open: true
    # depends_on:
    #   - serviceName
    volumes:
      - "/node_modules/.bin:/client/node_modules/.bin"
    ports:
      - 3000:3000
    environment:
      - REACT_APP_HOST=server
    networks:
      - api
  server:
    image: davidhailu0/mybusserver
    container_name: server
    environment:
      - MONGODB_URI=mongodb+srv://mybus:priuJr3FVQ96IcSB@cluster0.gn92lfx.mongodb.net/?retryWrites=true&w=majority
      - PORT=9000
      - HOST=localhost
    networks:
      - api
  test:
    build: 
      context: .
      dockerfile: dockerfile.test
    container_name: test
    ports:
      - 3000:3000
    environment:
      - REACT_APP_HOST=localserver
    networks:
      - api
networks:
  api: {}